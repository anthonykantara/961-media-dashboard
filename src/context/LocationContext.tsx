import React,{createContext,useContext,useEffect,useState,ReactNode}from'react';import{LocationTerritory,SupportedLanguage,SUPPORTED_LANGUAGES}from'../types/location';
const LEBANON:LocationTerritory={id:'lb',name:'Lebanon',slug:'lb',level:'country',countryCode:'LB',flagEmoji:'🇱🇧',isHub:false,status:'active',supportedLanguages:['en','ar','fr'],defaultLanguage:'en'};
interface Ctx{locations:LocationTerritory[];activeLocationId:string;activeLocation:LocationTerritory;activeLanguage:string;activeLanguageInfo:SupportedLanguage;availableLanguages:SupportedLanguage[];setActiveLocationId:(id:string|null)=>void;setActiveLanguage:(lang:string)=>void;setActiveLocationAndLanguage:(id:string|null,lang?:string)=>void;addLocation:(l:LocationTerritory)=>void;updateLocation:(l:LocationTerritory)=>void;deleteLocation:(id:string)=>void;smartDetectLocation:()=>Promise<LocationTerritory>;isDetecting:boolean;detectedMessage:string|null;getLocationUrl:(l:LocationTerritory|null,lang?:string)=>string;}
const C=createContext<Ctx|undefined>(undefined);
export function LocationProvider({children}:{children:ReactNode}){const[lang,setLang]=useState('en');const[detecting,setDetecting]=useState(false);const[msg,setMsg]=useState<string|null>(null);useEffect(()=>{document.documentElement.dir=(SUPPORTED_LANGUAGES[lang]||SUPPORTED_LANGUAGES.en).dir;document.documentElement.lang=lang},[lang]);const setActiveLanguage=(x:string)=>{if(LEBANON.supportedLanguages.includes(x))setLang(x)};const value:Ctx={locations:[LEBANON],activeLocationId:'lb',activeLocation:LEBANON,activeLanguage:lang,activeLanguageInfo:SUPPORTED_LANGUAGES[lang]||SUPPORTED_LANGUAGES.en,availableLanguages:LEBANON.supportedLanguages.map(x=>SUPPORTED_LANGUAGES[x]).filter(Boolean),setActiveLocationId:()=>{},setActiveLanguage,setActiveLocationAndLanguage:(_,l)=>{if(l)setActiveLanguage(l)},addLocation:()=>{},updateLocation:()=>{},deleteLocation:()=>{},smartDetectLocation:async()=>{setDetecting(true);setMsg('Lebanon');setDetecting(false);return LEBANON},isDetecting:detecting,detectedMessage:msg,getLocationUrl:(l,language=lang)=>l?`/${l.countryCode.toLowerCase()}/${language}`:`/${language}`};return <C.Provider value={value}>{children}</C.Provider>}
export function useLocationContext(){const c=useContext(C);if(!c)throw new Error('useLocationContext must be used within LocationProvider');return c}
